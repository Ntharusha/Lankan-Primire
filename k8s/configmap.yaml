apiVersion: v1
kind: ConfigMap
metadata:
  name: lankan-primire-config
  namespace: lankan-primire
data:
  NODE_ENV: "production"
  PORT: "5000"
  FRONTEND_URL: "http://lankan-primire.local"
  METRICS_ENABLED: "true"
